import React from "react";
import ReactDOM from "react-dom";
export function PortfolioList(props){
    
    
    
    return(
        
        <div>
            {/* <ul className="PortfolioUl">
                {stateData.portfolios.map(element => <li key={element.name}>{element.name}</li>)}
            </ul> */}
        </div>
    )
}